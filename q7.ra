loan2 := \rename_{loan2: no2, type2, minCredit2, amount2}(loan);
notMax := \project_{no}(
  \select_{amount < amount2}(
    loan \cross loan2
  )
);
\rename_{result: loanNo}(
  \project_{no}(loan) \diff notMax
)
