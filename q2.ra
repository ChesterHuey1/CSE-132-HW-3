jumbo :- \rename_{name} (
  \project_{cname} (
    borrower \join_{lno = no} (\select_{type = 'jumbo mortgage'} (loan))
  )
);
highCredit :- \project_{name} (\select_{credit >= 750} (customer));
jumbo \union highCredit;
